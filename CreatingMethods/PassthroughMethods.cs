namespace CreatingMethods
{
    // ReSharper disable once IdentifierTypo
    public static class PassthroughMethods
    {
        public static bool ReturnValue(bool boolValue)
        {
            return boolValue;
        }

        // TODO #1: Add a static method here with name "ReturnValue" that gets "byteValue" parameter ("byte" type) and returns it.
        public static byte ReturnValue(byte byteValue)
        {
            return byteValue;
        }

        // TODO #2: Add a static method here with name "ReturnValue" that gets "sbyteValue" parameter ("sbyte" type) and returns it.
        public static sbyte ReturnValue(sbyte sbyteValue)
        {
            return sbyteValue;
        }

        // TODO #3: Add a static method here with name "ReturnValue" that gets "charValue" parameter ("char" type) and returns it.
        public static char ReturnValue(char charValue)
        {
            return charValue;
        }

        // TODO #4: Add a static method here with name "ReturnValue" that gets "decimalValue" parameter ("decimal" type) and returns it.
        public static decimal ReturnValue(decimal decimalValue)
        {
            return decimalValue;
        }

        // TODO #5: Add a static method here with name "ReturnValue" that gets "doubleValue" parameter ("double" type) and returns it.
        public static double ReturnValue(double doubleValue)
        {
            return doubleValue;
        }

        // TODO #6: Add a static method here with name "ReturnValue" that gets "floatValue" parameter ("float" type) and returns it.
        public static float ReturnValue(float floatValue)
        {
            return floatValue;
        }

        // TODO #7: Add a static method here with name "ReturnValue" that gets "intValue" parameter ("int" type) and returns it.
        public static int ReturnValue(int intValue)
        {
            return intValue;
        }

        // TODO #8: Add a static method here with name "ReturnValue" that gets "uintValue" parameter ("uint" type) and returns it.
        public static uint ReturnValue(uint uintValue)
        {
            return uintValue;
        }

        // TODO #9: Add a static method here with name "ReturnValue" that gets "longValue" parameter ("long" type) and returns it.
        public static long ReturnValue(long longValue)
        {
            return longValue;
        }

        // TODO #10: Add a static method here with name "ReturnValue" that gets "ulongValue" parameter ("ulong" type) and returns it.
        public static ulong ReturnValue(ulong ulongValue)
        {
            return ulongValue;
        }

        // TODO #11: Add a static method here with name "ReturnValue" that gets "shortValue" parameter ("short" type) and returns it.
        public static short ReturnValue(short shortValue)
        {
            return shortValue;
        }

        // TODO #12: Add a static method here with name "ReturnValue" that gets "ushortValue" parameter ("ushort" type) and returns it.
        public static ushort ReturnValue(ushort ushortValue)
        {
            return ushortValue;
        }
    }
}
